# Absolute origin used to resolve OpenGraph and Twitter card image URLs.
#
# Optional on Vercel: the deployment URL is detected automatically, so previews
# and first deploys work without this. Set it once a real domain is attached,
# or link previews keep pointing at the .vercel.app address.
NEXT_PUBLIC_SITE_URL=https://your-domain.com
